/** Imported by `__tests__/trace-smoke-fixture.test.ts` so a traced run has a first-party module
 *  body to bracket. Deliberately trivial and dependency-free. */
export const tracedFixtureMarker = 'traced';
