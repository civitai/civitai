# Redis Inspection Tool Configuration
#
# This skill inspects Redis cache keys, values, and TTLs.
# Supports both main cache and system cache (--sys flag).

# Main cache URL (default)
# Used for regular cached data (may be cluster mode)
REDIS_URL=redis://user:password@localhost:6379

# System cache URL (for --sys flag)
# Used for persistent system values (single node)
REDIS_SYS_URL=redis://user:password@localhost:6379
