# Meilisearch Admin Tool Configuration
#
# This skill checks Meilisearch index status, tasks, health, and settings.
# Supports both main search and feed/metrics search (--feed flag).

# Main search configuration (default)
SEARCH_HOST=https://your-meilisearch-host.com
SEARCH_API_KEY=your_search_api_key_here

# Feed/metrics search configuration (for --feed flag)
# Optional - only needed if using feed mode
METRICS_SEARCH_HOST=https://your-metrics-search-host.com
METRICS_SEARCH_API_KEY=your_metrics_search_api_key_here
