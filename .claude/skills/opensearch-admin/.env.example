# OpenSearch Admin Tool Configuration
#
# Supports local Docker and remote instances.

# OpenSearch host (default: http://localhost:9200)
OPENSEARCH_HOST=http://localhost:9200

# Optional Bearer token for authenticated clusters
# OPENSEARCH_API_KEY=your_api_key_here
