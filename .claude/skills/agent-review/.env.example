# Agent Review Configuration
#
# This skill routes requests to different AI providers:
# - Anthropic models (opus, sonnet) -> Claude Agent SDK (uses local subscription)
# - Other models (gemini, gpt) -> OpenRouter API (requires key below)

# OpenRouter API key (required for non-Anthropic models)
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Default model to use (optional)
# Available: google/gemini-3-pro-preview, openai/gpt-5.1-codex, anthropic/claude-opus-4.5, anthropic/claude-sonnet-4.5
# Aliases: gemini, gpt, opus, sonnet, claude
# AGENT_REVIEW_DEFAULT_MODEL=google/gemini-3-pro-preview
