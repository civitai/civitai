// Test stub for `$app/environment`. Defaults to dev mode (the dev-login route is gated on `dev`); a test
// can override per-case with `vi.mock('$app/environment', () => ({ dev: false }))`.
export const dev = true;
